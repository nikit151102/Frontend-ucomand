version: '3.1'
services:
  frontend:
    build:
      context: ./Frontend-ucomand
      dockerfile: Dockerfile-frontend
    ports:
      - 80:80
    volumes:
      - ./Frontend-ucomand:/app
      - /app/node_modules



